# ¿Qué me llevo? V2 — TravelApps
V2 conectada al Worker de Cloudflare/Groq.

IMPORTANTE: para que la segunda fase (generación de checklist) funcione, el Worker debe aceptar el prompt enviado en `trip` y devolver el campo `packing_list` solicitado. La API key permanece solo en Cloudflare, nunca en GitHub.
